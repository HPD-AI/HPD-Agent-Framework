<Project>
  <Import Project="$([MSBuild]::GetPathOfFileAbove('Directory.Build.props', '$(MSBuildThisFileDirectory)../'))" />

  <PropertyGroup>
    <!-- HPD-Auth.Framework targets net10.0 only — packages require net10.0 -->
    <TargetFrameworks>net10.0</TargetFrameworks>
  </PropertyGroup>
</Project>
