# Bun configuration for HPD Agent Client

[install]
# Freeze lockfile in CI
frozen-lockfile = false

# Auto-install peer dependencies
auto = true

[install.cache]
# Use bun's native cache
disable = false

[test]
# Use bun test runner
preload = []
