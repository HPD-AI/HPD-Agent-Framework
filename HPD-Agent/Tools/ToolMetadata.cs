using System;

namespace HPD.Agent;

/// <summary>
/// Metadata about a container (tool, skill, or other), used for collapsing and organization.
/// Generated by the source generator for containers marked with [Collapse].
/// </summary>
public class ToolMetadata
{
    /// <summary>
    /// Container name (typically the class name).
    /// </summary>
    public string Name { get; set; } = string.Empty;

    /// <summary>
    /// Description of container capabilities (from [Collapse] attribute).
    /// </summary>
    public string Description { get; set; } = string.Empty;

    /// <summary>
    /// Names of all functions in this container.
    /// </summary>
    public string[] FunctionNames { get; set; } = Array.Empty<string>();

    /// <summary>
    /// Total number of functions in this container.
    /// </summary>
    public int FunctionCount { get; set; }

    /// <summary>
    /// Whether this container has the [Collapse] attribute.
    /// If true, the container should be collapsed (container generated).
    /// If false, all functions are always visible.
    /// </summary>
    public bool HasCollapseAttribute { get; set; }
}
